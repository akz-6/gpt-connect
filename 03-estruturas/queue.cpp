#include <bits/stdc++.h>
using namespace std;

int main() {
    queue<int> q;
    q.push(10);
    q.push(20);

    cout << q.front() << '\n';
    q.pop();

    // FIFO: primeiro que entra, primeiro que sai.
}